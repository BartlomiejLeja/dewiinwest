import { CarouselItemElement } from './carousel-item-element.directive';

describe('CarouselItemElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselItemElement();
    expect(directive).toBeTruthy();
  });
});
